<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Hack to allow us to use the Holo.Light theme, as it's currently 'private' in the base libs -->
    <style name="defaultStyle" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/defaultActionBarStyle</item>
    </style>

    <style name="defaultActionBarStyle" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">#CCCCCC</item>
    </style>

</resources>